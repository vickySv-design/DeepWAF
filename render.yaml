version: 1
services:
  - type: web
    name: deepwaf
    env: python
    buildCommand: pip install -r requirements.txt && python train_cnn.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
